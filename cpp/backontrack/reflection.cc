#include <iostream>
#include <string>
#include <vector>
#include <unordered_map>

// Structure to store class info
// struct ClassInfo {
//     std::string name;
//     std::vector<std::string> fields;
// };

// ✅ Declare registry BEFORE using the macro
std::unordered_map<std::string, std::string> aaaa;

// // ✅ Correctly define the macro AFTER registry is declared
// #define REGISTER_CLASS(className, ...) \
//     registry[#className] = {#className, std::vector<std::string>{__VA_ARGS__}}

// class Person {
// public:
//     std::string name;
//     int age;
// };

// ✅ Use the macro after registry is defined
  dfg dfg  dfg dfg dfgfdgdfgdfg  dfg dfg
int main() {
    aaaa["a"] = "string";df
dfg g dregistered dfdfgyg, infgeld << " ";
    //     }dfgdf dfg d
    // }fgdfg
    return 0;dfg
}
